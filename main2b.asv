img = imread('../data2/CT_1.jpg');
img_modified = uint8(zeros(1024));
the4=1023/254;
for x = 1:1024
    for y = 1:1024
        x1=floor((x-1)/the4+1);
        x2=floor((x-1)/the4+2);
        y1=floor((y-1)/the4+1);
        y2=floor((y-1)/the4+2);
        fR1=(x2-x)*img(x1,y1)+(x-x1)*img(x2,y1);
        fR2=(x2-x)*img(x1,y2)+(x-x1)*img(x2,y2);
        fP=(y2-y)*fR1+(y-y1)*fR2
    end
end
imshow(img_modified)